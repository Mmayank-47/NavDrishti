# Progress and evidence log

1. **Inspect:** clean `main` at `5fdda67`; legacy `data_loader.py` converts timestamp ms to seconds, uses fixed preprocessing rate 10 Hz, labels phone speed km/h and divides by 3.6. `run_revalidation_v4.py` falls back to vehicle/GNSS speed when NIO speed is absent. **Invariant:** this package has no dependency on that evaluator or speed fallback. **Red:** initial test collection failed because the isolated adapter was absent. **Fix:** independent canonical adapter/replay. **Negative control:** a constant-velocity replay with changed gyro/reference has identical trajectory. Real-data availability is assessed by the CLI, not fabricated.
2. **Inspect/specify:** phone gyro z is not assumed to be vehicle yaw. **Invariant:** gyro-heading requires a supplied phone-to-vehicle rotation and uses midpoint integration. **Red:** known-answer tests initially revealed test-contract defects; corrected without changing numerical tolerance. **Fix:** supplied rotation and finite input validation. **Negative control:** invalid gyro fails the gyro arm; CV declares gyro forbidden.
3. **Inspect/specify:** exact blackout endpoint must be scored before correction. **Invariant:** replay includes exact `t0`/`t1`, never applies a GNSS update. **Fix:** explicit independent replay boundary, `NOT_EVALUABLE` for missing reference, finite JSON export.
